import React from 'react';

export const ExportTool: React.FC<{ series: any }> = () => {
    return (
        <div style={{ marginTop: '2rem' }}>
            {/* AI and WhatsApp features temporarily removed */}
        </div>
    );
};
